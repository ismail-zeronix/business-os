/**
 * The standard lines of the "Terms in brief" block that the compose drawer writes into a quotation email. They are a starting point: the
 * person sees and can edit the whole message before pressing Send. Payment and delivery terms are not here; they come from the quotation
 * itself when it has them. Edit these to change what every new email starts with.
 */
export const QUOTATION_EMAIL = {
  standardTerms: ["Prices and stock availability are subject to confirmation at the time of order."],
};
